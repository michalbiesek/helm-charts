apiVersion: v1
kind: Service
metadata:
  name: {{ include "scope.fullname" . }}
  labels:
    {{- include "scope.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scope.selectorLabels" . | nindent 4 }}
