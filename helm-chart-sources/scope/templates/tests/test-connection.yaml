apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scope.fullname" . }}-test-connection"
  labels:
    {{- include "scope.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "scope.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
